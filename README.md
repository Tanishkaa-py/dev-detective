# dev-detective
Dev Detective is a responsive web app that lets users search GitHub profiles and view real-time data using the GitHub API. It displays user details, latest repositories, handles loading and error states gracefully, and is built using HTML, CSS, and vanilla JavaScript.
